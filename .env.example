# Copiá este archivo a ".env" (sin .example) y pegá tu key real ahí.
# El archivo .env real NUNCA se sube al repo (está en .gitignore).
#
#   cp .env.example .env
#
# Después editá .env y reemplazá lo de abajo con tu key de console.anthropic.com

ANTHROPIC_API_KEY=sk-ant-tu-key-aca
